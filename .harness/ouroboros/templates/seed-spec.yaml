# Seed Specification Template
# Generated by: /seed command
# WARNING: Seeds are immutable. To change, create a new version.

version: 1
created: "YYYY-MM-DDTHH:MM:SS"
interview_ref: ""

goal:
  summary: ""          # One clear sentence
  detail: ""           # 2-3 sentences expanding the goal
  non_goals:
    - ""               # What this project is NOT

constraints:
  must:                # Absolute requirements
    - ""
  must_not:            # Absolute prohibitions
    - ""
  should:              # Preferred but negotiable
    - ""

acceptance_criteria:
  - id: "AC-001"
    description: ""    # Must be testable
    verification: ""   # manual | automated | both
    priority: ""       # must | should | nice
    complexity: ""     # low | medium | high — determines Pair Mode activation
                       # low: 단순 CRUD, 설정, UI 스타일링 → Pair Mode 스킵
                       # medium: 비즈니스 로직, 데이터 변환, API 통합 → Pair Mode 선택적
                       # high: 결제/정산, 보안, 복잡한 상태 관리, 외부 연동 → Pair Mode 강제

ontology:
  entities:
    - name: ""
      fields:
        - name: ""
          type: ""     # string | number | boolean | date | reference
          required: true
      relationships:
        - target: ""
          type: ""     # has_many | belongs_to | has_one
  actions:
    - name: ""
      actor: ""        # Who performs this
      input: ""        # What goes in
      output: ""       # What comes out
      side_effects:
        - ""           # What changes

architecture:
  pattern: "3-tier-layered"   # layered | hexagonal | cqrs | microservices
  layers:
    presentation:
      description: ""          # e.g., "Next.js pages and API routes"
      directories: []          # e.g., ["src/app", "src/components"]
      responsibilities:
        - "HTTP request/response handling"
        - "Input validation and parsing"
        - "UI rendering"
      cannot_access: ["database", "external_services"]
    logic:
      description: ""          # e.g., "Service modules with business rules"
      directories: []          # e.g., ["src/services", "src/domain"]
      responsibilities:
        - "Business rules and validation"
        - "Transaction orchestration"
        - "Domain logic"
      cannot_access: []
    data:
      description: ""          # e.g., "Prisma repositories"
      directories: []          # e.g., ["src/repositories", "prisma"]
      responsibilities:
        - "Database operations"
        - "External API calls"
        - "Data serialization"
      cannot_access: ["presentation"]
  layer_communication:
    presentation_to_logic: ""  # e.g., "service injection" | "direct call"
    logic_to_data: ""          # e.g., "repository pattern" | "ORM direct"
    data_format: ""            # e.g., "DTO" | "interface" | "plain object"

scope:
  mvp:
    - ""               # What's in MVP
  future:
    - ""               # What's deferred

tech_decisions:
  - decision: ""
    reason: ""
    alternatives_considered:
      - ""
